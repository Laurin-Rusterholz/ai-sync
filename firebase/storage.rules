rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Firebase Admin in Netlify/n8n bypasses these rules. Browser clients must
    // always be authenticated, including for FlowerTech videos and PDFs.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
